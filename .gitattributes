# Normalize text files to LF in repo; convert to CRLF on checkout if user has core.autocrlf=true
*           text=auto

# Force LF for source and data
*.py        text eol=lf
*.sh        text eol=lf
*.sql       text eol=lf
*.md        text eol=lf
*.yml       text eol=lf
*.yaml      text eol=lf
*.toml      text eol=lf

# Keep PowerShell scripts with CRLF on checkout
*.ps1       text eol=crlf

# GitHub Linguist hints (must start at column 1)
data/vendor/**  linguist-vendored
data/derived/** linguist-vendored