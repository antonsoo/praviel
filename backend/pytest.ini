# pytest.ini (repo root)
[pytest]
testpaths = backend/app/tests
asyncio_mode = auto
addopts = -q

# You can comment these out if you prefer to see all warnings.
filterwarnings =
    ignore::pytest.PytestRemovedIn9Warning
    ignore::pytest.PytestDeprecationWarning
