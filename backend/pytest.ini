[pytest]
# Use a single asyncio loop for the entire test session so connections created in fixtures
# are used/closed on the same loop.
asyncio_mode = strict
asyncio_default_fixture_loop_scope = session

# Optional but nice for repo-root runs:
testpaths = backend/app/tests
