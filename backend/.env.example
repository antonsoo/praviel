# =============================================================================
# Ancient Languages App - Environment Configuration Template
# =============================================================================
# Copy this file to `.env` and fill in your actual values.
# The `.env` file is gitignored and should NEVER be committed.
#
# Usage:
#   1. cp .env.example .env
#   2. Edit .env with your actual credentials
#   3. Restart the backend server
# =============================================================================

# Database Configuration
DATABASE_URL=postgresql+asyncpg://app:app@localhost:5433/app
REDIS_URL=redis://localhost:6379/0

# Development Settings
ALLOW_DEV_CORS=1         # Enable CORS for local Flutter client in dev
EMBED_DIM=1536           # Embedding dimension (default: OpenAI text-embedding-3-small)

# Data Paths
DATA_VENDOR_ROOT=../data/vendor
DATA_DERIVED_ROOT=../data/derived

# Feature Flags
LESSONS_ENABLED=1
BYOK_ENABLED=1           # Bring-Your-Own-Key support
SERVE_FLUTTER_WEB=1
# TTS_ENABLED=1          # Text-to-speech (uncomment to enable)

# =============================================================================
# Vendor API Keys (Server-Side)
# =============================================================================
# These are used when BYOK is disabled or as fallback providers.
# Obtain keys from the respective provider dashboards:
#
# OpenAI: https://platform.openai.com/api-keys
# Anthropic: https://console.anthropic.com/settings/keys
# Google AI: https://makersuite.google.com/app/apikey
# =============================================================================

OPENAI_API_KEY=sk-proj-your-openai-key-here
ANTHROPIC_API_KEY=sk-ant-api03-your-anthropic-key-here
GOOGLE_API_KEY=your-google-api-key-here

# Echo Fallback Control
# Set to 'true' to return mock responses when real providers fail (useful for testing)
# Set to 'false' to show real errors (recommended for development)
ECHO_FALLBACK_ENABLED=false
